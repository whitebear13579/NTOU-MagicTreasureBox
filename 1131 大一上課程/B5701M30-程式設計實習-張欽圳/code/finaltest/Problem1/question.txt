The following program generates some integer numbers at random and finds the unique elements of these numbers.  Answer the following questions regarding the two functions: main and unique. 

1. 
在函式unique裡，從#1行至#2行基本上是二分搜尋法. 請指出#1行與#2行
(In the function unique, the codes from Line #1 to Line #2 basically implement the binary search algorithm. Please, identify Line #1 and Line #2.)
Line #1:   (1)    (>35) 39
Line #2:   (2)    (<51) 49

2.
選出下面敘述的兩個目的。(你必須查realloc的用途。)
(What are the two main purposes of the following statements? You have to find the definition of the function realloc.)
```c
initial_size*=2;
vc.value = (int*)realloc(vc.value,sizeof(int)*initial_size);
vc.count = (unsigned*)realloc(vc.count,sizeof(unsigned)*initial_size);
```

A. 增加動態配置記憶體 (increase the size of the dynamically allocated memory)
B. 釋放動態配置記憶體 (release the size of the dynamically allocated memory)
C. 減少動態配置記憶體 (reduce the size of the dynamically allocated memory)
D. 搬移動態配置記憶體內容至新記憶體位置 (move the memory block to a new location)

A D

3.
main產生多少整數
Show the number of elements generated at random by the function main:(1)
80000

main產生多少不同的整數
Show the number of unique integer numbers generated by the function main:(2)
9998

陣列data裡最小整數是多少
Show the value of the smallest element in the array data:(3)
0

陣列data裡最大整數是多少
Show the value of the largest element in the array data:(4)
9999

同一個數字最多出現幾次
Show the maximum number of times that a number appears in the array data:(5)
20

出現最多次數的數字有幾個
Show the number of elements appearing most frequently:(6)
1

在函式unique裡，vc.value配置了多少integer元素
Show the number of integer elements dynamically allocated for vc.value in the function unique:(7)
16384

在函式unique裡，vc.count配置了多少unsigned元素
Show the number of unsigned elements dynamically allocated for vc.count in the function unique:(8)
16384